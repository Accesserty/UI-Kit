# Tasks

## Implementation Order

1. [x] Create `src/components/rating.js` with `AuRating` unified component
2. [x] Implement `AuRating` interactive logic
   - Radio group pattern with keyboard navigation (←↑→↓)
   - SVG star icons with stable DOM structure
   - Labels below each star
   - Form integration (name, value)
3. [x] Implement score display features
   - Optional numeric score text (`show-score`)
   - Custom info suffix (`score-info`)
   - Partial star fill for fractional values
4. [x] Add CSS variables for customization
5. [x] Create `demo/rating.html` with usage examples
6. [x] Create `test/rating.test.js` with comprehensive tests
7. [x] Update `demo/index.html` to include rating demo link
8. [x] Validate with `openspec validate`
